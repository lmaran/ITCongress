app.controller('whiteListController', function ($scope) {
    $scope.whiteList = [];

    //init();

    //$scope.addCustomer = function () {
    //    $scope.customers.push(
    //    {
    //        name: $scope.newCustomer.name,
    //        city: $scope.newCustomer.city
    //    });
    //};

    function init() {
        //$scope.users = homeService.getCustomers();
    };

});